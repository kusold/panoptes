import Widget from 'panoptes-icon-title'
import icon from './cadvisor.png';

const CAdvisor = props => (
	<Widget backgroundColor='#08f' icon={icon} {...props}>CAdvisor</Widget>
);

export default CAdvisor;
